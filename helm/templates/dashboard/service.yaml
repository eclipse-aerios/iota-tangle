{{- if .Values.dashboard.services.nodePort }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dashboard.name" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.dashboard.services.nodePort.type }}
  ports:
    {{- range .Values.dashboard.services.nodePort.ports }}
    - name: {{ .name }}
      port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol }}
      nodePort: {{ .nodePort }}
    {{- end }}
  selector:
    {{- include "dashboard.selectorLabels" . | nindent 4 }}
{{- end }}

